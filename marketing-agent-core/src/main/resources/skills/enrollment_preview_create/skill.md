# enrollment_preview_create

Prepare the diff and confirmation card for a future enrollment execution. This worker is proposal-only: it may create a pending action, but it must not write business state or call real enrollment APIs.