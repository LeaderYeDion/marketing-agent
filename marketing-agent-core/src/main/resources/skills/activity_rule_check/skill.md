# activity_rule_check

Assess activity-rule compatibility from explicit activity context and upstream observations. This capability should not execute enrollment. When evidence is weak, it must expose limitations or missing inputs so the harness can ask, fallback, or re-plan later.
