# spreadsheet_summarize

Read a spreadsheet-like file and turn its structure into an observation. This worker is read-only and should be used before downstream product lookup, rule checks, preview creation, or notification copywriting when the user's goal depends on a registration Excel.